#pragma once

#include <string>

namespace solvers {
unsigned Solve(const std::string& input_file, const std::string& output_file,
               const std::string& task_name);
}  // namespace solvers
